name: Flutter CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.5' # deine gewünschte Version

      - name: Get dependencies
        run: flutter pub get

      - name: Run tests
        run: flutter test
      - name: Build debug apk
        run: flutter build apk
